'use client'

import Link from 'next/link'
import { usePathname, useRouter } from 'next/navigation'
import { useMemo, useState } from 'react'
import { Heart, Menu, Minus, Plus, Search, ShoppingBag, Star, X, ArrowRight, ChevronDown, Mail, MapPin, Phone, Check } from 'lucide-react'
import { categories, cartSubtotal, editorialImage, findProduct, formatPrice, getCartCount, getCartItems, heroImage, products, relatedProducts, testimonials, type CartItem, type Category, type Product } from '@/lib/store-data'

const navItems = [
  { label: 'Shop', href: '/shop' },
  { label: 'Bedding', href: '/shop?category=Bedding' },
  { label: 'Pillows', href: '/shop?category=Pillows' },
  { label: 'Duvets', href: '/shop?category=Duvets' },
  { label: 'Our story', href: '/about' },
]

export function StorefrontShell({ children }: { children: React.ReactNode }) {
  const [cart, setCart] = useState<CartItem[]>([])
  const [wishlist, setWishlist] = useState<string[]>([])
  const [cartOpen, setCartOpen] = useState(false)
  const [menuOpen, setMenuOpen] = useState(false)
  const [toast, setToast] = useState('')
  const router = useRouter()
  const pathname = usePathname()
  const count = getCartCount(cart)

  const notify = (message: string) => {
    setToast(message)
    window.setTimeout(() => setToast(''), 2600)
  }

  const addToCart = (productId: string, size?: string, color?: string) => {
    const product = findProduct(productId)
    if (!product) return
    const item = { productId, quantity: 1, size: size ?? product.sizes[0], color: color ?? product.colors[0] }
    setCart((current) => {
      const existing = current.find((entry) => entry.productId === productId && entry.size === item.size && entry.color === item.color)
      if (existing) return current.map((entry) => entry === existing ? { ...entry, quantity: entry.quantity + 1 } : entry)
      return [...current, item]
    })
    notify(`${product.name} added to your bag`)
  }

  const updateQuantity = (index: number, amount: number) => {
    setCart((current) => current.flatMap((item, itemIndex) => itemIndex !== index ? item : item.quantity + amount > 0 ? [{ ...item, quantity: item.quantity + amount }] : []))
  }

  const toggleWishlist = (productId: string) => {
    setWishlist((current) => current.includes(productId) ? current.filter((id) => id !== productId) : [...current, productId])
    notify(wishlist.includes(productId) ? 'Removed from wishlist' : 'Saved to wishlist')
  }

  return (
    <div className="min-h-screen bg-background text-foreground">
      <AnnouncementBar />
      <Header count={count} menuOpen={menuOpen} setMenuOpen={setMenuOpen} onCart={() => setCartOpen(true)} />
      {menuOpen && <MobileMenu onClose={() => setMenuOpen(false)} />}
      <main>{pathname === '/shop' ? <ShopPage onAdd={addToCart} /> : children}</main>
      <Footer />
      <CartDrawer open={cartOpen} onClose={() => setCartOpen(false)} cart={cart} updateQuantity={updateQuantity} onCheckout={() => { setCartOpen(false); router.push('/checkout') }} />
      {toast && <div role="status" className="fixed bottom-5 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-full bg-primary px-5 py-3 text-sm text-primary-foreground shadow-xl"><Check className="size-4" />{toast}</div>}

    </div>
  )
}

function AnnouncementBar() {
  return <div className="bg-primary px-4 py-2 text-center font-mono text-[10px] uppercase tracking-[0.22em] text-primary-foreground">Complimentary delivery on orders over ₦150,000</div>
}

function Header({ count, menuOpen, setMenuOpen, onCart }: { count: number; menuOpen: boolean; setMenuOpen: (value: boolean) => void; onCart: () => void }) {
  return <header className="sticky top-0 z-40 border-b border-border/60 bg-background/95 backdrop-blur-md">
    <div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-10">
      <button className="rounded-full p-2 md:hidden" aria-label={menuOpen ? 'Close menu' : 'Open menu'} onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X /> : <Menu />}</button>
      <Link href="/" className="font-serif text-2xl font-semibold tracking-tight text-primary md:text-[28px]">fabulous<span className="font-sans text-[11px] font-medium uppercase tracking-[0.35em] text-accent-foreground"> beddings</span></Link>
      <nav className="hidden items-center gap-7 md:flex">{navItems.map((item) => <Link key={item.href} href={item.href} className="text-[11px] font-medium uppercase tracking-[0.15em] text-muted-foreground transition-colors hover:text-primary">{item.label}</Link>)}</nav>
      <div className="flex items-center gap-1"><Link href="/shop" aria-label="Search" className="rounded-full p-2.5 transition-colors hover:bg-muted"><Search className="size-[18px]" /></Link><Link href="/wishlist" aria-label="Wishlist" className="hidden rounded-full p-2.5 transition-colors hover:bg-muted sm:block"><Heart className="size-[18px]" /></Link><button onClick={onCart} aria-label={`Shopping bag with ${count} items`} className="relative rounded-full p-2.5 transition-colors hover:bg-muted"><ShoppingBag className="size-[18px]" />{count > 0 && <span className="absolute right-0 top-0 flex size-4 items-center justify-center rounded-full bg-accent text-[9px] font-bold text-accent-foreground">{count}</span>}</button></div>
    </div>
  </header>
}

function MobileMenu({ onClose }: { onClose: () => void }) {
  return <div className="border-b border-border bg-background px-6 py-5 md:hidden"><nav className="flex flex-col gap-4">{navItems.map((item) => <Link key={item.href} href={item.href} onClick={onClose} className="font-serif text-xl">{item.label}</Link>)}</nav><div className="mt-6 flex gap-4 border-t border-border pt-5"><Link href="/contact" onClick={onClose} className="text-sm text-muted-foreground">Contact</Link><Link href="/wishlist" onClick={onClose} className="text-sm text-muted-foreground">Wishlist</Link></div></div>
}

function Footer() {
  return <footer className="mt-20 bg-primary px-5 py-14 text-primary-foreground lg:px-10"><div className="mx-auto max-w-7xl"><div className="grid gap-10 md:grid-cols-[1.4fr_1fr_1fr_1.4fr]"><div><Link href="/" className="font-serif text-3xl">fabulous</Link><p className="mt-4 max-w-xs text-sm leading-6 text-primary-foreground/70">Beautiful bedding for the rituals that make a house feel like home.</p><div className="mt-6 flex gap-3"><a href="#" aria-label="Instagram" className="rounded-full border border-primary-foreground/25 px-3 py-2 font-mono text-[10px]">ig</a><a href="#" aria-label="Facebook" className="rounded-full border border-primary-foreground/25 px-3 py-2 font-mono text-[10px]">fb</a></div></div><div><h2 className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary-foreground/50">Explore</h2><nav className="mt-4 flex flex-col gap-3 text-sm"><Link href="/shop">Shop all</Link><Link href="/shop?category=Bedding">Bedding</Link><Link href="/shop?category=Accessories">Accessories</Link><Link href="/about">Our story</Link></nav></div><div><h2 className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary-foreground/50">Help</h2><nav className="mt-4 flex flex-col gap-3 text-sm"><Link href="/contact">Contact us</Link><Link href="/shipping">Shipping & returns</Link><Link href="/faq">FAQs</Link><Link href="/size-guide">Size guide</Link></nav></div><div><h2 className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary-foreground/50">Stay in the know</h2><p className="mt-4 text-sm leading-6 text-primary-foreground/70">Seasonal notes, new arrivals and thoughtful home inspiration.</p><form className="mt-4 flex border-b border-primary-foreground/40 pb-2" onSubmit={(event) => event.preventDefault()}><input aria-label="Email address" placeholder="Your email address" type="email" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-primary-foreground/45" /><button aria-label="Subscribe"><ArrowRight className="size-4" /></button></form></div></div><div className="mt-14 flex flex-col gap-3 border-t border-primary-foreground/15 pt-5 text-[10px] uppercase tracking-[0.16em] text-primary-foreground/45 sm:flex-row sm:items-center sm:justify-between"><span>© 2024 Fabulous Beddings</span><span>Made for slow mornings</span></div></div></footer>
}

function CartDrawer({ open, onClose, cart, updateQuantity, onCheckout }: { open: boolean; onClose: () => void; cart: CartItem[]; updateQuantity: (index: number, amount: number) => void; onCheckout: () => void }) {
  const items = getCartItems(cart)
  if (!open) return null
  return <div className="fixed inset-0 z-50"><button className="absolute inset-0 bg-primary/35" aria-label="Close cart" onClick={onClose} /><aside className="absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-background shadow-2xl"><div className="flex items-center justify-between border-b border-border px-6 py-5"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground">Your selection</p><h2 className="mt-1 font-serif text-2xl">Shopping bag</h2></div><button onClick={onClose} aria-label="Close shopping bag" className="rounded-full p-2 hover:bg-muted"><X className="size-5" /></button></div><div className="flex-1 overflow-y-auto px-6 py-5">{items.length ? <div className="flex flex-col gap-5">{items.map(({ product, quantity, size, color }, index) => product && <div key={`${product.id}-${size}-${color}`} className="flex gap-4"><img src={product.image} alt={product.name} className="size-24 rounded-md object-cover" /><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="truncate text-sm font-medium">{product.name}</h3><p className="mt-1 text-xs text-muted-foreground">{size} · {color}</p></div><button aria-label={`Remove ${product.name}`} onClick={() => updateQuantity(index, -quantity)} className="text-muted-foreground"><X className="size-4" /></button></div><div className="mt-4 flex items-center justify-between"><div className="flex items-center border border-border"><button onClick={() => updateQuantity(index, -1)} aria-label="Decrease quantity" className="p-1.5"><Minus className="size-3" /></button><span className="w-7 text-center text-xs">{quantity}</span><button onClick={() => updateQuantity(index, 1)} aria-label="Increase quantity" className="p-1.5"><Plus className="size-3" /></button></div><span className="text-sm font-medium">{formatPrice(product.price * quantity)}</span></div></div></div>)}</div> : <div className="flex h-full flex-col items-center justify-center text-center"><ShoppingBag className="size-10 text-muted-foreground/50" /><h3 className="mt-5 font-serif text-2xl">Your bag is empty</h3><p className="mt-2 max-w-xs text-sm leading-6 text-muted-foreground">Take a look around and find something beautiful for your bedroom.</p><Link href="/shop" onClick={onClose} className="mt-6 bg-primary px-5 py-3 text-xs font-medium uppercase tracking-[0.14em] text-primary-foreground">Start shopping</Link></div>}</div>{items.length > 0 && <div className="border-t border-border px-6 py-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Subtotal</span><span className="font-serif text-xl">{formatPrice(cartSubtotal(cart))}</span></div><p className="mt-2 text-xs text-muted-foreground">Taxes and delivery calculated at checkout.</p><button onClick={onCheckout} className="mt-5 flex w-full items-center justify-center gap-2 bg-primary py-4 text-xs font-medium uppercase tracking-[0.14em] text-primary-foreground">Continue to checkout <ArrowRight className="size-4" /></button></div>}</aside></div>
}

export function Hero() {
  return <section className="relative isolate min-h-[600px] overflow-hidden bg-secondary lg:min-h-[650px]"><img src={heroImage} alt="A softly made bed in a sunlit bedroom" className="absolute inset-0 size-full object-cover object-center" /><div className="absolute inset-0 bg-primary/35" /><div className="relative mx-auto flex min-h-[600px] max-w-7xl items-end px-5 pb-16 lg:min-h-[650px] lg:px-10 lg:pb-20"><div className="max-w-xl text-primary-foreground"><p className="font-mono text-[10px] uppercase tracking-[0.28em]">The art of a beautiful night</p><h1 className="mt-5 font-serif text-5xl leading-[0.95] tracking-tight sm:text-6xl lg:text-8xl">Your softest<br /><em className="font-normal">place to land.</em></h1><p className="mt-6 max-w-sm text-sm leading-6 text-primary-foreground/85">Thoughtfully made bedding for bedrooms that feel like a deep breath.</p><Link href="/shop" className="mt-8 inline-flex items-center gap-3 bg-primary-foreground px-6 py-4 text-xs font-medium uppercase tracking-[0.16em] text-primary transition-colors hover:bg-accent">Shop the collection <ArrowRight className="size-4" /></Link></div></div><div className="absolute bottom-6 right-6 hidden items-center gap-3 text-primary-foreground/80 md:flex"><span className="h-px w-12 bg-primary-foreground/50" /><span className="font-mono text-[10px] uppercase tracking-[0.2em]">Scroll to explore</span></div></section>
}

export function CategorySection() {
  return <section className="mx-auto max-w-7xl px-5 py-20 lg:px-10 lg:py-28"><div className="flex items-end justify-between gap-5"><div><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-accent-foreground">Find your comfort</p><h2 className="mt-3 font-serif text-4xl tracking-tight sm:text-5xl">Shop by feeling</h2></div><Link href="/shop" className="hidden items-center gap-2 text-xs font-medium uppercase tracking-[0.14em] md:flex">View all <ArrowRight className="size-4" /></Link></div><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-4 md:gap-5">{categories.map((category, index) => <Link key={category.name} href={`/shop?category=${category.name}`} className={`group relative aspect-[0.8] overflow-hidden bg-muted ${index === 0 ? 'col-span-2 md:col-span-1' : ''}`}><img src={category.image} alt={category.name} className="absolute inset-0 size-full object-cover transition-transform duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-primary/20 transition-colors group-hover:bg-primary/35" /><div className="absolute inset-x-4 bottom-4 text-primary-foreground"><p className="font-serif text-2xl">{category.name}</p><p className="mt-1 font-mono text-[10px] uppercase tracking-[0.16em] opacity-80">{category.count}</p></div></Link>)}</div><Link href="/shop" className="mt-7 flex items-center justify-center gap-2 text-xs font-medium uppercase tracking-[0.14em] md:hidden">View all products <ArrowRight className="size-4" /></Link></section>
}

export function FeaturedProducts({ onAdd }: { onAdd?: (id: string) => void }) {
  return <section className="bg-secondary/50 px-5 py-20 lg:px-10 lg:py-28"><div className="mx-auto max-w-7xl"><div className="flex items-end justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-accent-foreground">Quiet luxuries</p><h2 className="mt-3 font-serif text-4xl tracking-tight sm:text-5xl">The considered edit</h2></div><Link href="/shop" className="hidden items-center gap-2 text-xs font-medium uppercase tracking-[0.14em] md:flex">Shop all <ArrowRight className="size-4" /></Link></div><div className="mt-10 grid gap-x-4 gap-y-10 sm:grid-cols-2 lg:grid-cols-4">{products.filter((product) => product.featured).map((product) => <ProductCard key={product.id} product={product} onAdd={onAdd} />)}</div></div></section>
}

export function ProductCard({ product, onAdd, wishlist, onWishlist }: { product: Product; onAdd?: (id: string) => void; wishlist?: boolean; onWishlist?: (id: string) => void }) {
  return <article className="group"><div className="relative aspect-[0.88] overflow-hidden bg-muted"><Link href={`/product/${product.id}`}><img src={product.image} alt={product.name} className="size-full object-cover transition-transform duration-700 group-hover:scale-105" /></Link>{product.badge && <span className="absolute left-3 top-3 bg-primary px-3 py-1.5 font-mono text-[9px] uppercase tracking-[0.12em] text-primary-foreground">{product.badge}</span>}{onWishlist && <button aria-label={wishlist ? `Remove ${product.name} from wishlist` : `Add ${product.name} to wishlist`} onClick={() => onWishlist(product.id)} className="absolute right-3 top-3 rounded-full bg-background/90 p-2.5 transition-colors hover:bg-background"><Heart className={`size-4 ${wishlist ? 'fill-accent text-accent' : ''}`} /></button>}<button onClick={() => onAdd?.(product.id)} className="absolute inset-x-3 bottom-3 translate-y-14 bg-background py-3 text-xs font-medium uppercase tracking-[0.12em] opacity-0 transition-all group-hover:translate-y-0 group-hover:opacity-100">Quick add</button></div><Link href={`/product/${product.id}`} className="block"><div className="mt-4 flex items-start justify-between gap-3"><div><h3 className="text-sm font-medium">{product.name}</h3><div className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><Star className="size-3 fill-accent text-accent" /> {product.rating} <span>({product.reviews})</span></div></div><div className="text-right text-sm"><span>{formatPrice(product.price)}</span>{product.compareAt && <span className="ml-2 text-xs text-muted-foreground line-through">{formatPrice(product.compareAt)}</span>}</div></div></Link></article>
}

export function EditorialSection() {
  return <section className="mx-auto max-w-7xl px-5 py-20 lg:px-10 lg:py-28"><div className="grid items-center gap-10 md:grid-cols-2 md:gap-16"><div className="relative aspect-[0.95] overflow-hidden"><img src={editorialImage} alt="A quiet bedroom with layered neutral bedding" className="size-full object-cover" /><div className="absolute bottom-5 left-5 bg-background px-4 py-3"><span className="font-mono text-[9px] uppercase tracking-[0.18em]">Since 2018 · Lagos</span></div></div><div className="max-w-md"><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-accent-foreground">Made for the everyday</p><h2 className="mt-4 font-serif text-4xl leading-tight sm:text-5xl">A softer way to live.</h2><p className="mt-6 text-sm leading-7 text-muted-foreground">We believe the bedroom is more than a room. It is where the day begins, where it ends, and where you return to yourself. Our bedding is designed to make that return feel a little more beautiful.</p><Link href="/about" className="mt-8 inline-flex items-center gap-3 border-b border-primary pb-2 text-xs font-medium uppercase tracking-[0.16em]">Our story <ArrowRight className="size-4" /></Link></div></div></section>
}

export function TestimonialSection() {
  return <section className="border-y border-border bg-secondary/50 px-5 py-20 lg:px-10 lg:py-24"><div className="mx-auto max-w-5xl text-center"><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-accent-foreground">The good word</p><div className="mt-10 grid gap-8 md:grid-cols-3">{testimonials.map((item) => <figure key={item.name} className="flex flex-col items-center"><div className="flex gap-1 text-accent"><Star className="size-3 fill-current" /><Star className="size-3 fill-current" /><Star className="size-3 fill-current" /><Star className="size-3 fill-current" /><Star className="size-3 fill-current" /></div><blockquote className="mt-5 font-serif text-xl leading-7">“{item.quote}”</blockquote><figcaption className="mt-5 font-mono text-[10px] uppercase tracking-[0.16em] text-muted-foreground">{item.name} · {item.location}</figcaption></figure>)}</div></div></section>
}

export function NewsletterSection() {
  return <section className="bg-accent px-5 py-16 text-accent-foreground lg:px-10"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-8 md:flex-row md:items-center"><div><p className="font-mono text-[10px] uppercase tracking-[0.24em]">A note from us</p><h2 className="mt-3 font-serif text-3xl sm:text-4xl">Make room for beautiful things.</h2></div><form className="flex w-full max-w-md border-b border-accent-foreground/50 pb-3" onSubmit={(event) => event.preventDefault()}><input type="email" required aria-label="Your email address" placeholder="Your email address" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-accent-foreground/60" /><button aria-label="Subscribe"><ArrowRight className="size-5" /></button></form></div></section>
}

export function ShopPage({ onAdd }: { onAdd?: (id: string) => void }) {
  const [category, setCategory] = useState<Category | 'All'>('All')
  const [sort, setSort] = useState('featured')
  const filtered = useMemo(() => { const list = category === 'All' ? products : products.filter((product) => product.category === category); return [...list].sort((a, b) => sort === 'low' ? a.price - b.price : sort === 'high' ? b.price - a.price : 0) }, [category, sort])
  const [mobileFilters, setMobileFilters] = useState(false)
  return <div><section className="bg-secondary px-5 py-14 lg:px-10 lg:py-20"><div className="mx-auto max-w-7xl"><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-accent-foreground">The collection</p><h1 className="mt-4 max-w-2xl font-serif text-5xl tracking-tight sm:text-6xl">Beautiful things for better rest.</h1><p className="mt-5 max-w-md text-sm leading-6 text-muted-foreground">Explore our considered collection of bedding, pillows and finishing touches.</p></div></section><section className="mx-auto max-w-7xl px-5 py-10 lg:px-10"><div className="flex items-center justify-between border-b border-border pb-5"><button onClick={() => setMobileFilters(!mobileFilters)} className="flex items-center gap-2 text-xs font-medium uppercase tracking-[0.14em] md:hidden">Filter <ChevronDown className={`size-4 transition-transform ${mobileFilters ? 'rotate-180' : ''}`} /></button><p className="hidden text-sm text-muted-foreground md:block">{filtered.length} pieces</p><div className="flex items-center gap-3"><p className="hidden text-sm text-muted-foreground sm:block">Sort by</p><select value={sort} onChange={(event) => setSort(event.target.value)} aria-label="Sort products" className="bg-transparent text-xs font-medium uppercase tracking-[0.12em] outline-none"><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div></div><div className={`${mobileFilters ? 'flex' : 'hidden'} flex-wrap gap-2 border-b border-border py-5 md:flex`}>{(['All', 'Bedding', 'Pillows', 'Duvets', 'Accessories'] as const).map((item) => <button key={item} onClick={() => setCategory(item)} className={`border px-4 py-2 text-xs uppercase tracking-[0.12em] ${category === item ? 'border-primary bg-primary text-primary-foreground' : 'border-border hover:border-primary'}`}>{item}</button>)}</div><div className="mt-8 grid gap-x-4 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((product) => <ProductCard key={product.id} product={product} onAdd={onAdd} />)}</div></section></div>
}

export function ProductPage({ product }: { product: Product }) {
  const [selectedSize, setSelectedSize] = useState(product.sizes[0])
  const [selectedColor, setSelectedColor] = useState(product.colors[0])
  const [quantity, setQuantity] = useState(1)
  const [added, setAdded] = useState(false)
  return <div><div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><Link href="/shop" className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground hover:text-primary">← Back to collection</Link><div className="mt-8 grid gap-10 md:grid-cols-2 md:gap-16"><div className="aspect-[0.92] overflow-hidden bg-muted"><img src={product.image} alt={product.name} className="size-full object-cover" /></div><div className="flex flex-col justify-center"><div className="flex items-center gap-2 text-xs text-muted-foreground"><span className="flex items-center gap-1 text-accent-foreground"><Star className="size-3 fill-current" /> {product.rating}</span> · {product.reviews} reviews</div><h1 className="mt-4 font-serif text-4xl leading-tight sm:text-5xl">{product.name}</h1><div className="mt-5 flex items-center gap-3"><span className="text-lg">{formatPrice(product.price)}</span>{product.compareAt && <span className="text-sm text-muted-foreground line-through">{formatPrice(product.compareAt)}</span>}</div><p className="mt-6 max-w-md text-sm leading-7 text-muted-foreground">{product.description}</p><div className="mt-8 border-t border-border pt-6"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em]">Size</p><div className="mt-3 flex flex-wrap gap-2">{product.sizes.map((size) => <button key={size} onClick={() => setSelectedSize(size)} className={`border px-4 py-2 text-xs ${selectedSize === size ? 'border-primary bg-primary text-primary-foreground' : 'border-border'}`}>{size}</button>)}</div></div><div className="mt-6"><p className="font-mono text-[10px] uppercase tracking-[0.16em]">Colour · {selectedColor}</p><div className="mt-3 flex gap-2">{product.colors.map((color) => <button key={color} onClick={() => setSelectedColor(color)} aria-label={`Select ${color}`} className={`size-7 rounded-full border-2 ${selectedColor === color ? 'border-primary ring-2 ring-accent ring-offset-2' : 'border-background ring-1 ring-border'}`} style={{ backgroundColor: color.toLowerCase() === 'pearl' ? '#eee9df' : color.toLowerCase() === 'sand' ? '#c8b59c' : color.toLowerCase() === 'mauve' ? '#ae8a8e' : color.toLowerCase() === 'blush' ? '#d6a9a0' : color.toLowerCase() === 'cocoa' ? '#6e5143' : color.toLowerCase() === 'olive' ? '#818066' : '#f7f4ef' }} />)}</div></div></div><div className="mt-8 flex gap-3"><div className="flex items-center border border-border"><button onClick={() => setQuantity(Math.max(1, quantity - 1))} aria-label="Decrease quantity" className="px-3"><Minus className="size-4" /></button><span className="w-8 text-center text-sm">{quantity}</span><button onClick={() => setQuantity(quantity + 1)} aria-label="Increase quantity" className="px-3"><Plus className="size-4" /></button></div><button onClick={() => setAdded(true)} className="flex flex-1 items-center justify-center gap-2 bg-primary px-5 py-4 text-xs font-medium uppercase tracking-[0.14em] text-primary-foreground">{added ? <><Check className="size-4" /> Added to bag</> : <>Add to bag <ShoppingBag className="size-4" /></>}</button><button aria-label="Save to wishlist" className="border border-border px-4"><Heart className="size-5" /></button></div><div className="mt-8 flex flex-col gap-3 border-t border-border pt-6 text-xs text-muted-foreground"><p>Free delivery on orders over ₦150,000</p><p>Made with {product.material.toLowerCase()}</p><p>Easy returns within 14 days</p></div></div></div></div><section className="mt-16 border-t border-border bg-secondary/40 px-5 py-16 lg:px-10"><div className="mx-auto max-w-7xl"><p className="font-mono text-[10px] uppercase tracking-[0.24em]">You may also like</p><div className="mt-8 grid gap-5 sm:grid-cols-3">{relatedProducts(product).map((item) => <ProductCard key={item.id} product={item} />)}</div></div></section></div>
}

export function CartPage() {
  const [cart, setCart] = useState<CartItem[]>([{ productId: 'satin-cloud-set', quantity: 1, size: 'King', color: 'Pearl' }])
  const items = getCartItems(cart)
  return <div className="mx-auto max-w-7xl px-5 py-12 lg:px-10 lg:py-20"><div className="flex items-end justify-between border-b border-border pb-6"><div><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-muted-foreground">Your selection</p><h1 className="mt-3 font-serif text-5xl">Shopping bag</h1></div><span className="text-sm text-muted-foreground">{getCartCount(cart)} item</span></div><div className="mt-10 grid gap-12 lg:grid-cols-[1fr_360px]">{items.length ? <div className="flex flex-col gap-6">{items.map(({ product, quantity, size, color }, index) => product && <div key={product.id} className="flex gap-5 border-b border-border pb-6"><img src={product.image} alt={product.name} className="size-32 object-cover sm:size-40" /><div className="flex flex-1 flex-col justify-between gap-4"><div className="flex justify-between gap-4"><div><h2 className="font-serif text-2xl">{product.name}</h2><p className="mt-1 text-sm text-muted-foreground">{size} · {color}</p></div><span>{formatPrice(product.price * quantity)}</span></div><div className="flex items-center justify-between"><div className="flex items-center border border-border"><button onClick={() => setCart((current) => current.flatMap((item, itemIndex) => itemIndex !== index ? item : item.quantity > 1 ? [{ ...item, quantity: item.quantity - 1 }] : []))} className="p-2" aria-label="Decrease quantity"><Minus className="size-3" /></button><span className="w-7 text-center text-xs">{quantity}</span><button onClick={() => setCart((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, quantity: item.quantity + 1 } : item))} className="p-2" aria-label="Increase quantity"><Plus className="size-3" /></button></div><button onClick={() => setCart([])} className="text-xs uppercase tracking-[0.12em] text-muted-foreground hover:text-primary">Remove</button></div></div></div>)}</div> : <div className="py-16 text-center"><ShoppingBag className="mx-auto size-10 text-muted-foreground/50" /><h2 className="mt-5 font-serif text-3xl">Your bag is empty</h2><Link href="/shop" className="mt-6 inline-flex bg-primary px-5 py-3 text-xs uppercase tracking-[0.12em] text-primary-foreground">Shop the collection</Link></div>}<aside className="h-fit bg-secondary p-6"><h2 className="font-serif text-2xl">Order summary</h2><div className="mt-6 flex flex-col gap-3 border-b border-border pb-5 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Subtotal</span><span>{formatPrice(cartSubtotal(cart))}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Delivery</span><span>{cartSubtotal(cart) >= 150000 ? 'Complimentary' : 'Calculated at checkout'}</span></div></div><div className="mt-5 flex justify-between font-serif text-xl"><span>Total</span><span>{formatPrice(cartSubtotal(cart))}</span></div><Link href="/checkout" className="mt-6 flex items-center justify-center gap-2 bg-primary py-4 text-xs uppercase tracking-[0.14em] text-primary-foreground">Checkout <ArrowRight className="size-4" /></Link></aside></div></div>
}

export function CheckoutPage() {
  const [placed, setPlaced] = useState(false)
  if (placed) return <div className="mx-auto flex min-h-[60vh] max-w-xl flex-col items-center justify-center px-5 py-20 text-center"><div className="flex size-14 items-center justify-center rounded-full bg-accent"><Check className="size-6" /></div><h1 className="mt-7 font-serif text-5xl">Thank you.</h1><p className="mt-4 max-w-sm text-sm leading-6 text-muted-foreground">Your order is on its way to becoming part of your home. We&apos;ll send your confirmation details shortly.</p><Link href="/shop" className="mt-8 bg-primary px-6 py-4 text-xs uppercase tracking-[0.14em] text-primary-foreground">Continue shopping</Link></div>
  return <div className="mx-auto max-w-6xl px-5 py-12 lg:px-10 lg:py-20"><Link href="/cart" className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground">← Back to bag</Link><div className="mt-8 grid gap-12 lg:grid-cols-[1fr_350px]"><div><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-muted-foreground">Almost home</p><h1 className="mt-3 font-serif text-5xl">Checkout</h1><form className="mt-10 flex flex-col gap-8" onSubmit={(event) => { event.preventDefault(); setPlaced(true) }}><fieldset className="flex flex-col gap-4"><legend className="font-serif text-2xl">Contact details</legend><input required type="email" placeholder="Email address" aria-label="Email address" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /></fieldset><fieldset className="flex flex-col gap-4"><legend className="font-serif text-2xl">Delivery address</legend><div className="grid gap-4 sm:grid-cols-2"><input required placeholder="First name" aria-label="First name" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><input required placeholder="Last name" aria-label="Last name" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /></div><input required placeholder="Street address" aria-label="Street address" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><div className="grid gap-4 sm:grid-cols-2"><input required placeholder="City" aria-label="City" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><select aria-label="State" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none"><option>Lagos</option><option>Abuja</option><option>Rivers</option></select></div><input required placeholder="Phone number" aria-label="Phone number" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /></fieldset><button className="bg-primary px-6 py-4 text-xs font-medium uppercase tracking-[0.14em] text-primary-foreground sm:self-start">Place order <ArrowRight className="ml-2 inline size-4" /></button></form></div><aside className="h-fit bg-secondary p-6"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground">Your order</p><div className="mt-6 flex items-center gap-4 border-b border-border pb-5"><img src={products[0].image} alt={products[0].name} className="size-16 object-cover" /><div className="flex-1"><p className="text-sm">{products[0].name}</p><p className="mt-1 text-xs text-muted-foreground">King · Pearl · Qty 1</p></div><span className="text-sm">{formatPrice(products[0].price)}</span></div><div className="mt-5 flex justify-between font-serif text-xl"><span>Total</span><span>{formatPrice(products[0].price)}</span></div></aside></div></div>
}

export function InfoPage({ type }: { type: 'about' | 'contact' }) {
  if (type === 'contact') return <div className="mx-auto max-w-7xl px-5 py-16 lg:px-10 lg:py-24"><div className="grid gap-12 md:grid-cols-2"><div><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-accent-foreground">We&apos;d love to hear from you</p><h1 className="mt-4 font-serif text-6xl">Say hello.</h1><p className="mt-6 max-w-sm text-sm leading-7 text-muted-foreground">Whether you need help finding the right size or just want to talk bedding, our small team is here for you.</p><div className="mt-10 flex flex-col gap-5 text-sm"><p className="flex items-center gap-3"><Mail className="size-4 text-accent-foreground" /> hello@fabulousbeddings.com</p><p className="flex items-center gap-3"><Phone className="size-4 text-accent-foreground" /> +234 803 000 0000</p><p className="flex items-center gap-3"><MapPin className="size-4 text-accent-foreground" /> Lagos, Nigeria</p></div></div><form className="flex flex-col gap-5 bg-secondary p-6 sm:p-10" onSubmit={(event) => event.preventDefault()}><h2 className="font-serif text-3xl">Send a message</h2><input required placeholder="Your name" aria-label="Your name" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><input required type="email" placeholder="Email address" aria-label="Email address" className="border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><textarea required placeholder="How can we help?" aria-label="Your message" rows={4} className="resize-none border-b border-border bg-transparent px-0 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary" /><button className="mt-3 bg-primary py-4 text-xs uppercase tracking-[0.14em] text-primary-foreground">Send message</button></form></div></div>
  return <div><section className="bg-secondary px-5 py-20 lg:px-10 lg:py-28"><div className="mx-auto max-w-7xl"><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-accent-foreground">Our story · Lagos, Nigeria</p><h1 className="mt-5 max-w-3xl font-serif text-5xl leading-tight sm:text-7xl">The bedroom is where we return to ourselves.</h1></div></section><section className="mx-auto grid max-w-7xl gap-10 px-5 py-20 lg:grid-cols-2 lg:gap-20 lg:px-10 lg:py-28"><img src={editorialImage} alt="A sunlit bedroom styled with neutral textiles" className="aspect-[0.85] w-full object-cover" /><div className="flex flex-col justify-center"><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-accent-foreground">A softer way to live</p><h2 className="mt-4 font-serif text-4xl">Beautiful bedding is a daily ritual.</h2><div className="mt-6 flex flex-col gap-5 text-sm leading-7 text-muted-foreground"><p>Fabulous began with a simple belief: that the things we use every day deserve to be as considered as the things we save for special occasions.</p><p>We source fabrics that feel wonderful against the skin, work with makers who care about the details, and design pieces that make home feel more like yours.</p><p>Because a good night&apos;s sleep is not an indulgence. It is the foundation for everything else.</p></div></div></section></div>
}

function ShopActions({ addToCart, wishlist, toggleWishlist }: { addToCart: (id: string) => void; wishlist: string[]; toggleWishlist: (id: string) => void }) {
  void addToCart; void wishlist; void toggleWishlist
  return null
}
